apiVersion: 1

datasources:
  - name: TimescaleDB
    type: postgres
    url: timescaledb:5432
    database: bluesky
    user: bluesky
    secureJsonData:
      password: bluesky
    jsonData:
      sslmode: disable
      postgresVersion: 1600
      timescaledb: true
    isDefault: true

  - name: Loki
    type: loki
    url: http://loki:3100
    isDefault: false
